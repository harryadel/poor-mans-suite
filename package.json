{
  "name": "poor-mans-suite",
  "version": "1.0.0",
  "description": "Poor Man's Suite is a Chrome DevTools extension for capturing, modifying, and replaying HTTP requests with optional AI-assisted security analysis.",
  "license": "GPL-3.0-or-later",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:ui": "vitest --ui",
    "package": "node scripts/package.js",
    "build": "npm test && npm run package"
  },
  "devDependencies": {
    "vitest": "^1.0.0",
    "jsdom": "^23.0.0"
  }
}
